apiVersion: v1
kind: Secret
metadata:
  name: booklibrary-secret
  namespace: production
type: Opaque
metadata:
  annotations:
    note: "Do not store real values here. Use sealed-secrets or CI secrets."
stringData:
  DB_USER: "REPLACE_ME"
  DB_PASSWORD: "REPLACE_ME"
